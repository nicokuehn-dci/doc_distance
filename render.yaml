services:
  - type: web
    name: doc-distance-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn project.wsgi --log-file -"
    # No managed DB; the app uses SQLite locally (not needed on Render)
    plan: free
