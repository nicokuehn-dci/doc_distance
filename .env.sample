# .env.sample — copy to .env for local development
# Do NOT commit real secrets. Use Render Dashboard to set production env vars.

# Django secret key
SECRET_KEY=replace_with_a_strong_secret_key

# Debug mode (True/False)
DEBUG=True

# Allowed hosts (comma-separated)
ALLOWED_HOSTS=127.0.0.1,localhost

# Disable collectstatic during deploy if you don't want Render to run it
DISABLE_COLLECTSTATIC=1

# Optional: database URL
# DATABASE_URL=sqlite:///db.sqlite3

# Add any other env vars your app needs below
